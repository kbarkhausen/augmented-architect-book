# Paths requiring human review
paths:
  high_risk:
    - 'src/auth/**'
    - 'src/billing/**'
    - 'src/admin/**'
    - 'migrations/**'
    - '.github/workflows/**'
    
  requires_review:
    - 'package.json'
    - 'tsconfig.json'
    - '.env.example'