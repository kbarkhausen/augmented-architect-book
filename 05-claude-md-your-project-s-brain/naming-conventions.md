## Naming Conventions

✅ Good:
- getUserById(id: string)
- createPaymentIntent(amount: number)
- validateEmailFormat(email: string)

❌ Bad:
- getUser(id) // Missing parameter type
- create(data) // Too vague
- validateEmail(e) // Cryptic parameter name