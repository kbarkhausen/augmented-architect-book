# In Claude Code:

> Write failing tests for a user registration function that:
> - Validates email format
> - Checks password strength (min 8 chars, 1 number, 1 special)
> - Prevents duplicate emails
> - Returns appropriate error messages

# Claude generates tests FIRST, all failing

> Now implement the function to make these tests pass

# Claude implements with tests as spec